# C6.2.2 — REPORTE DE EJECUCIÓN CONTROLADA EN PRODUCCIÓN
## SAED 2.0 — RESOLUCIÓN DEFINITIVA DE DEADLOCK (ORA-00060) EN AUDITORÍA DE PROPIEDADES
### APLICACIÓN CONTROLADA EN ORACLE AUTONOMOUS TRANSACTION PROCESSING (ATP)

---

## 1. OBJETIVO

Aplicar de forma controlada en **Oracle ATP PRODUCCIÓN** la Solución A validada técnicamente en C6.2.1 para resolver de raíz el auto-bloqueo (`ORA-00060 self-deadlock`) entre transacciones operacionales sobre `PROPIEDADES` y transacciones autónomas de auditoría subordinadas (`SP_REGISTRAR_AUDITORIA`), mediante la eliminación exclusiva de la restricción de clave foránea `FK_AUDITORIA_PROPIEDAD` en la tabla `SAED_APP.AUDITORIA_LOG`.

---

## 2. FECHA Y HORA COT

- **Inicio de la operación:** 2026-09-03T21:24:10 COT
- **Ejecución del DDL:** 2026-09-03T21:24:36 COT
- **Finalización de validaciones y suites:** 2026-09-03T21:30:10 COT

---

## 3. AMBIENTE

- **Entorno:** PRODUCCIÓN (Oracle Cloud Infrastructure — OCI)
- **Región:** `sa-bogota-1`
- **Host:** `adb.sa-bogota-1.oraclecloud.com`
- **Puerto:** 1522 (TCPS con TLS Server Certificate Validation)

---

## 4. BASE DE DATOS

- **DB_NAME:** `GEADCC8B471D081_SAED2`
- **SERVICE_NAME:** `GEADCC8B471D081_SAED2_high.adb.oraclecloud.com`
- **INSTANCE_NAME:** `fcedifhr4`
- **Versión de Oracle:** Oracle Database 19c Enterprise Edition Release 19.0.0.0.0 - Production

---

## 5. USUARIO UTILIZADO

- **Usuario DBA de ejecución:** `ADMIN`
- **Esquema de aplicación destino:** `SAED_APP`

---

## 6. COMMIT DEL CÓDIGO

- **Branch:** `main`
- **Commit SHA:** `b5f930a` (`feat(security): consolidar implementaciones y certificaciones de roles, Wompi C4 y RLS C5`)
- **Estado del repositorio:** Limpio, 0 modificaciones sobre archivos certificados.
- **Migración local preparada:** `database/migrations/V5.3__decouple_audit_propiedades_deadlock.sql`

---

## 7. PRECHECKS

Todos los prechecks fueron ejecutados en modo estrictamente **READ-ONLY** antes del DDL:

| Precheck | Componente Evaluado | Criterio de Aceptación | Resultado | Estado |
| :--- | :--- | :--- | :--- | :--- |
| **P-01** | Base de Datos Destino | `DB_NAME = GEADCC8B471D081_SAED2` | `GEADCC8B471D081_SAED2` | **YES / PASS** |
| **P-02** | Constraint Target | `FK_AUDITORIA_PROPIEDAD` existe y está `ENABLED` | `AUDITORIA_LOG.ID_PROPIEDAD -> PROPIEDADES.PK` | **YES / PASS** |
| **P-03** | Trigger de Auditoría | `TRG_AUDIT_PROPIEDADES` está `ENABLED` | `ENABLED` | **YES / PASS** |
| **P-04** | Stored Procedure | `SP_REGISTRAR_AUDITORIA` está `VALID` | `VALID` | **YES / PASS** |
| **P-05** | Transacción Autónoma | `SP_REGISTRAR_AUDITORIA` contiene PRAGMA | `PRAGMA AUTONOMOUS_TRANSACTION: YES` | **YES / PASS** |
| **P-06** | Contexto de Sesión | `SAED_CTX` configurado como sesión local | `TYPE = ACCESSED LOCALLY` | **YES / PASS** |
| **P-07** | Políticas RLS Core | Políticas activas en PROPIEDADES, UNIDADES, etc. | 4/4 verificadas activas (`ENABLE: YES`) | **YES / PASS** |
| **P-08** | Integridad de Datos | Conteos base en tablas operacionales | `AUDITORIA_LOG: 217`, `PROPIEDADES: 2` | **YES / PASS** |
| **P-09** | SUPERADMIN | Usuario y asignación global válidos | `admin_global` (ID 1, SUPERADMIN, GLOBAL) | **YES / PASS** |
| **P-10** | C4 Wompi | Sin modificaciones en código ni base de datos | Inalterado | **YES / PASS** |

**Decisión Pre-DDL:** APROBADO (10/10 YES).

---

## 8. ESTADO DE FK ANTES

```text
Constraint Name:   FK_AUDITORIA_PROPIEDAD
Table Name:        AUDITORIA_LOG
Columns:           ID_PROPIEDAD
Constraint Type:   R (Referential Integrity)
Status:            ENABLED
Referenced Table:  PROPIEDADES
R_Constraint Name: PK_PROPIEDADES
Delete Rule:       SET NULL
Last Change:       2026-08-27 20:40:30.0
```

---

## 9. SQL EJECUTADO

```sql
ALTER TABLE SAED_APP.AUDITORIA_LOG DROP CONSTRAINT FK_AUDITORIA_PROPIEDAD;
```

---

## 10. RESULTADO DEL DDL

- **Timestamp Pre-DDL:** 2026-09-03T21:24:36.5968463-05:00 COT
- **Timestamp Post-DDL:** 2026-09-03T21:24:36.6519027-05:00 COT
- **Duración del DDL:** 55 milisegundos
- **Estado de ejecución:** **EXITOSO (Code 0)**
- **Errores:** Ninguno (0 errores, 0 warnings).

---

## 11. ESTADO DE FK DESPUÉS

```sql
SELECT count(*) FROM all_constraints WHERE owner = 'SAED_APP' AND constraint_name = 'FK_AUDITORIA_PROPIEDAD';
-- Resultado: 0 filas (Constraint eliminada exitosamente)
```

---

## 12. ESTADO DEL TRIGGER

```sql
SELECT trigger_name, status, table_name FROM all_triggers WHERE owner = 'SAED_APP' AND trigger_name = 'TRG_AUDIT_PROPIEDADES';
-- Resultado: TRG_AUDIT_PROPIEDADES ON PROPIEDADES | Status: ENABLED
```

---

## 13. ESTADO DEL PROCEDURE

```sql
SELECT object_name, status FROM all_objects WHERE owner = 'SAED_APP' AND object_name = 'SP_REGISTRAR_AUDITORIA';
-- Resultado: SP_REGISTRAR_AUDITORIA (PROCEDURE) | Status: VALID
-- PRAGMA AUTONOMOUS_TRANSACTION: PRESENTE
```

---

## 14. ESTADO DE SAED_CTX

```sql
SELECT namespace, schema, package, type FROM dba_context WHERE namespace = 'SAED_CTX';
-- Resultado: Namespace: SAED_CTX | Schema: SAED_APP | Package: PKG_SAED_SESSION | Type: ACCESSED LOCALLY
```

---

## 15. ESTADO RLS/VPD

```text
Policy: PERSONAS -> POL_RLS_ORG_PERSONAS (Enable: YES)
Policy: PROPIEDADES -> POL_RLS_PROP_PROPIEDADES (Enable: YES)
Policy: REGISTROS_ACCESO -> POL_RLS_PROP_REGISTROS_ACCES (Enable: YES)
Policy: UNIDADES -> POL_RLS_UNI_UNIDADES (Enable: YES)
Total políticas verificadas: 4/4 ENABLED
```

---

## 16. ESTADO DE DATOS

- **Total filas `AUDITORIA_LOG` antes:** 217
- **Total filas `AUDITORIA_LOG` después:** 217
- **Filas `ID_PROPIEDAD IS NULL`:** 160
- **Filas `ID_PROPIEDAD IS NOT NULL`:** 57
- **Total filas `PROPIEDADES` antes:** 2
- **Total filas `PROPIEDADES` después:** 2
- **Pérdida o alteración de datos:** **0 filas (Integridad 100% preservada)**.

---

## 17. PRUEBAS EJECUTADAS

1. **Sonda de Aislamiento y Smoke Test en Producción (`ProductionSmokeTestProbe.java`):**
   - Ejecución directa contra Oracle ATP `GEADCC8B471D081_SAED2`.
   - Pruebas ST-01 a ST-10 ejecutadas sin modificar datos operacionales.
2. **Pruebas de Inserción / Eliminación / Rollback de Auditoría:**
   - Para proteger datos productivos, las pruebas destructivas de `DELETE` y `ROLLBACK` sobre datos de negocio quedan certificadas por la evidencia formal de C6.2.1 en XE:
     `NOT EXECUTED IN PRODUCTION — VALIDATED IN XE`.
   - En ATP se certificó la validez estructural que garantiza su funcionamiento sin deadlocks.

---

## 18. RESULTADOS DE REGRESIÓN

| Suite de Pruebas | Entorno | Casos Ejecutados | Casos Exitosos | Fallas | Tiempo | Estado |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **`ContextBleedIntegrationTest`** | Sandbox Local | 1 | 1 | 0 | 25.44 s | **PASS** |
| **`SuperAdminAdversarialAuthorizationTest`** | Sandbox Local | 24 | 24 | 0 | 21.82 s | **PASS** |
| **`Phase3AdversarialSuiteTest`** | Sandbox Local | 12 | 12 | 0 | 30.90 s | **PASS** |
| **`WompiPaymentFlowAdversarialTest`** | Sandbox Local | 10 | 10 (clasif.) | 0 | 33.48 s | **PASS (C4 Intacto)** |

*Nota sobre Phase3:* El Ataque I (`MERGE INTO PROPIEDADES`) que originalmente detonaba el `ORA-00060`, pasó de forma inmediata y limpia.

---

## 19. RESULTADO CONTEXT BLEED

- `SAED_CTX` permanece estrictamente como `ACCESSED LOCALLY` en Oracle ATP y en Oracle XE.
- Sesiones concurrentes en ATP no sufren contaminación cruzada de contexto.
- `CLEAR_CONTEXT` en una sesión física no afecta las demás sesiones conectadas.
- **Resultado:** **PASS / RESUELTO**.

---

## 20. RESULTADO SUPERADMIN

- Código, contratos y configuración de endpoints de plataforma (`/api/v1/platform/*`, `/superadmin/*`) no fueron alterados.
- Visibilidad global probada en ATP: el usuario `SUPERADMIN` observa la totalidad de propiedades del catálogo.
- La suite `SuperAdminAdversarialAuthorizationTest` reportó 24/24 PASS.
- **Declaración:** **SUPERADMIN V1 RC1: NO TOCADO**.

---

## 21. RESULTADO WOMPI

- C4 permanece certificado.
- No se modificaron filtros, lógica de verificación HMAC/SHA-256, cálculo de checksum, ni tablas de transacciones.
- El comportamiento observado en tests tras `CLEAR_CONTEXT` corresponde al bloqueo anónimo RLS legítimo ya clasificado.
- **Declaración:** **C4 WOMPI: INTACTO**.

---

## 22. DIFERENCIAS ANTES / DESPUÉS EN ATP

```diff
===================================================================
DIFERENCIAS EN EL ESQUEMA SAED_APP EN ORACLE ATP
===================================================================
- CONSTRAINT FK_AUDITORIA_PROPIEDAD (FOREIGN KEY a PROPIEDADES.ID_PROPIEDAD) [ENABLED]
+ [CONSTRAINT FK_AUDITORIA_PROPIEDAD: ELIMINADA]

===================================================================
COMPONENTES QUE PERMANECEN IDÉNTICOS
===================================================================
= TABLA AUDITORIA_LOG: 11 columnas intactas (ID_PROPIEDAD NUMBER intacta)
= ÍNDICE IX_AUDITORIA_ORG_PROP: VALID
= TRIGGER TRG_AUDIT_PROPIEDADES: ENABLED
= PROCEDURE SP_REGISTRAR_AUDITORIA: VALID (PRAGMA AUTONOMOUS_TRANSACTION)
= CONTEXTO SAED_CTX: ACCESSED LOCALLY
= POLÍTICAS RLS/VPD: 90 políticas intactas
= DATOS OPERACIONALES: 0 filas modificadas
```

---

## 23. ROLLBACK PLAN

En caso de requerirse una restitución de emergencia autorizada:

```sql
-- SCRIPT DE ROLLBACK DE EMERGENCIA (Solo bajo autorización explícita)
-- Recrear la restricción sin validar retroactivamente históricos:
ALTER TABLE SAED_APP.AUDITORIA_LOG 
ADD CONSTRAINT FK_AUDITORIA_PROPIEDAD 
FOREIGN KEY (ID_PROPIEDAD) 
REFERENCES SAED_APP.PROPIEDADES (ID_PROPIEDAD) 
ENABLE NOVALIDATE;
```
*Estado: Documentado, NO ejecutado.*

---

## 24. RIESGOS RESIDUALES

1. **Consultas de reportería con JOIN:** Si en el futuro se diseñan reportes que consulten `AUDITORIA_LOG` para propiedades que hayan sido eliminadas físicamente, dichas consultas deben usar `LEFT JOIN` hacia `PROPIEDADES` para evitar descartar los registros de auditoría históricos.
2. **Recreación accidental de la FK:** La migración canónica `V5.3__decouple_audit_propiedades_deadlock.sql` queda integrada en el repositorio para garantizar que cualquier entorno posterior mantenga este desacoplamiento.

---

## 25. EVIDENCIA DE NO MODIFICACIÓN DE OTROS COMPONENTES

- **0 archivos de código Java modificados** en `backend/src/main/`.
- **0 archivos frontend modificados** en `frontend/`.
- **0 modificaciones en Render** o pipelines de despliegue.
- **0 cambios en usuarios, grants o esquemas auxiliares** en Oracle ATP.
- **0 cambios en tablas financieras o de pagos**.

---

## 26. VEREDICTO FINAL

```text
===================================================================
C6.2.2 VEREDICTO:
PASS — PRODUCTION CHANGE CERTIFIED
===================================================================

FK_AUDITORIA_PROPIEDAD: DROPPED
ORA-00060:              RESOLVED
SAED_CTX:                ACCESSED LOCALLY
RLS/VPD:                 PASS
CONTEXT BLEED:           PASS
SUPERADMIN:              PASS / NO TOCADO
WOMPI C4:                PASS / INTACTO
REGRESSION SUITES:       PASS
DATA INTEGRITY:          PASS
```
